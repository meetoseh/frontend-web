export type Emotion = {
  /**
   * The word describing the emotion, e.g., angry
   */
  word: string;
  /**
   * The present tense of the verb used to resolve the emotion, e.g., calm down
   */
  antonym: string;
};
